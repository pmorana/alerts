<h1>Event Page</h1>
<p>This page shows the event just created</p>

<p>
	<strong>Title:</strong>
	<%= @event.title %>
</p>

<p><strong>Takes place on: </strong><%= @event.event_date_time %>
</p>

<p>
	<strong>Info:</strong>
	<%= @event.info %>
</p>

<h2>Comments (hacky chat)</h2>
<%= render @event.chats %>

<h2>Add a comment: (chat faked)</h2>
<%= render 'chats/form' %>

<%= link_to 'Back', events_path %>
