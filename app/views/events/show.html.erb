<h1>Event Page</h1>
<p>This page shows the event just created</p>

<p>
	<strong>Title:</strong>
	<%= @event.title %>
</p>

<p><strong>Starts: </strong><%= @event.event_start %></p>
<p><strong>Ends: </strong><%= @event.event_end %></p>


<p>
	<strong>Info:</strong>
	<%= @event.info %>
</p>

<h2>Comments (hacky chat)</h2>
<%= render @event.chats %>

<h2>Add a comment: (chat faked)</h2>
<%= render 'chats/form' %>

<%= link_to 'Back', events_path %>
