<h1>Edit Event</h1>
<p>This will be the place to edit an existing Event</p>

<%= form_for :event, method: :patch do |f| %>
	<% if @event.errors.any? %>
	<div id="error_explanation">
		<h2>
			<%= pluralize(@event.errors.count, "error") %> prohibited this event from being saved: 
		</h2>
		<ul>
			<% @event.errors.full_messages.each do |msg| %>
			<li><%= msg %></li>
			<%end%>
		</ul>
	</div>
	<%end%>

	<p>
		<%= f.label :title %><br>
		<%= f.text_field :title %>
	</p>

	<p>
		<%= f.label :info %><br>
		<%= f.text_area :info %>
	</p>

	<p>
		<%= f.submit %>
	</p>
<%end%>

<%= link_to 'Back', events_path %>